// Package embed exists to allow Go code to access the file contents
// from the standard library. It contains the flux standard library
// as text that can be embedded into a Go binary.
package embed
