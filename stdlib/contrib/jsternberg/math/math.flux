package math

builtin minIndex
min = (values) => {
    index = minIndex(values)
    return values[index]
}

builtin maxIndex
max = (values) => {
	index = maxIndex(values)
	return values[index]
}

builtin sum
